apiVersion: v1
kind: Service
metadata:
  name: testprepsmallhello
  labels:
    app: testprepsmallhello
spec:
  type: ClusterIP
  selector:
    app: testprepsmallhello
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
